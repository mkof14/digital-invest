# Исправление проблемы с изображениями

## Проблема
Изображения проектов не отображались, потому что система пыталась загрузить WebP и AVIF форматы, которые еще не были сгенерированы.

## Что было исправлено

### 1. Логика определения современных форматов
- **До**: Система пыталась загрузить WebP/AVIF для всех изображений
- **После**: Современные форматы загружаются только для файлов из `/lovable-uploads/`, где они могут существовать
- **Результат**: Vite-импортированные изображения из `src/assets/` теперь работают без попыток загрузить несуществующие форматы

### 2. Обработка ошибок в OptimizedImage
- Добавлена проверка на пустые srcSet строки
- Добавлен обработчик `onError` для логирования проблем
- Улучшен fallback на оригинальные изображения

## Текущее состояние

✅ **Работает сейчас:**
- Изображения из `src/assets/projects/` загружаются через Vite импорты
- OptimizedImage корректно показывает оригинальные JPG файлы
- Blur placeholders и loading states работают

⏳ **Для production:**
- WebP и AVIF форматы будут сгенерированы автоматически при `npm run build` на Vercel
- Responsive версии (400w, 800w, 1200w, 1600w) нужно генерировать вручную

## Генерация современных форматов

### Автоматическая генерация (при деплое)

```bash
# На Vercel автоматически:
npm run build
# ↳ Vite plugin генерирует WebP/AVIF для всех изображений
```

### Ручная генерация (для тестирования)

```bash
# Генерировать современные форматы
node scripts/generate-modern-formats.js

# Генерировать responsive версии
node scripts/generate-responsive-images.js

# Проверить результат
ls -lh src/assets/projects/*.webp
ls -lh src/assets/projects/*-400w.webp
```

## Структура файлов после генерации

```
src/assets/projects/
├── digitalinvest-hero.jpg          ← Оригинал
├── digitalinvest-hero.webp         ← WebP версия
├── digitalinvest-hero.avif         ← AVIF версия
├── digitalinvest-hero-400w.jpg     ← Responsive 400px
├── digitalinvest-hero-400w.webp    ← Responsive 400px WebP
├── digitalinvest-hero-400w.avif    ← Responsive 400px AVIF
├── digitalinvest-hero-800w.jpg     ← Responsive 800px
├── digitalinvest-hero-800w.webp    ← ...
└── ...
```

## Как это работает сейчас

### Development (локально)
1. Vite импортирует изображения из `src/assets/projects/`
2. `getResponsiveImagePaths()` определяет, что это Vite-ассеты
3. Возвращает `undefined` для WebP/AVIF srcSet
4. OptimizedImage показывает только оригинальный JPG
5. **Результат**: Изображения работают без современных форматов ✓

### Production (на Vercel)
1. Vite plugin запускается при `npm run build`
2. Генерирует WebP и AVIF для каждого изображения
3. Оптимизирует и копирует в `dist/assets/`
4. `getResponsiveImagePaths()` определяет production build
5. OptimizedImage загружает современные форматы
6. **Результат**: Оптимизированные изображения с 60-85% экономией размера ✓

## Проверка работоспособности

### 1. Локально (сейчас)
```bash
npm run dev
# Откройте http://localhost:8080/projects
# ✅ Изображения должны показываться
```

### 2. Production build (локальный тест)
```bash
npm run build
npm run preview
# Откройте http://localhost:4173/projects
# ✅ Должны загружаться WebP/AVIF форматы
```

### 3. Chrome DevTools проверка
```
1. F12 → Network → Img
2. Перезагрузите страницу
3. Проверьте Type колонку:
   - Dev: jpeg
   - Production: webp или avif
```

## База данных - неправильные пути

⚠️ В базе данных есть неправильные пути:

```sql
-- НЕПРАВИЛЬНО (файлы не существуют):
hero_image_url = '/lovable-uploads/digitalinvest-hero.jpg'
hero_image_url = '/src/assets/projects/biomathlife-hero.jpg'

-- ПРАВИЛЬНО (но не обязательно):
hero_image_url = NULL  -- код использует slug для поиска импортированного изображения
```

**Решение**: Функция `getProjectImage()` в коде сначала проверяет импортированные изображения по slug, поэтому неправильные пути в БД игнорируются. Все работает через маппинг в коде.

## Рекомендации

### Перед деплоем на Vercel

**Опция 1: Автоматическая генерация (рекомендуется)**
```bash
git push
# Vercel автоматически сгенерирует WebP/AVIF при сборке
```

**Опция 2: Pre-генерация для быстрой сборки**
```bash
# Сгенерировать локально
node scripts/generate-modern-formats.js
node scripts/generate-responsive-images.js

# Закоммитить
git add src/assets/
git commit -m "Add pre-generated optimized images"
git push
```

### Для новых изображений

1. Добавьте оригинал в `src/assets/projects/name.jpg`
2. Импортируйте в `Projects.tsx`:
   ```typescript
   import nameHero from '@/assets/projects/name.jpg';
   ```
3. Добавьте в маппинг:
   ```typescript
   const projectImages: Record<string, string> = {
     'project-slug': nameHero,
   };
   ```
4. При деплое современные форматы сгенерируются автоматически

## Мониторинг производительности

### Ожидаемые метрики после деплоя:

| Метрика | Значение |
|---------|----------|
| Размер изображений | -60-85% от оригинала |
| LCP (Largest Contentful Paint) | < 2.5s |
| Performance Score | 90+ |
| Формат изображений | WebP на Chrome, AVIF на новых браузерах |

### Инструменты проверки:
- Chrome DevTools → Lighthouse
- Chrome DevTools → Network → Img
- Vercel Analytics (после включения)

## Что дальше?

1. ✅ **Изображения работают** - проблема решена
2. 🚀 **Готово к деплою** - Vercel сгенерирует оптимизации
3. 📊 **Мониторинг** - проверьте метрики после деплоя
4. 🎨 **Опционально** - сгенерируйте responsive версии для еще лучшей производительности

---

## Troubleshooting

### Изображения все еще не показываются?

**Проверьте консоль:**
```javascript
// Откройте DevTools → Console
// Должны видеть импортированные пути:
console.log(digitalinvestHero); 
// → "/src/assets/projects/digitalinvest-hero.jpg"
```

**Проверьте Network:**
```
DevTools → Network → Img
Статус должен быть 200, не 404
```

**Если все еще проблема:**
```bash
# Очистите кэш Vite
rm -rf node_modules/.vite
npm run dev
```

### WebP/AVIF не загружаются в production?

```bash
# Проверьте, что Sharp установлен:
npm list sharp
# Должно показать: sharp@0.34.5

# Проверьте build:
npm run build
# В логах должно быть: "Modern Image Format Generation"
```

---

**Статус**: ✅ Изображения работают, система готова к деплою с автоматической оптимизацией.
