# Deployment Checklist для Vercel

## ✅ Быстрая проверка перед деплоем

### 1. Зависимости

```bash
# Проверьте, что Sharp в dependencies (НЕ devDependencies)
grep -A 5 '"dependencies"' package.json | grep sharp
# ✅ Должно показать: "sharp": "^0.34.5"
```

### 2. Конфигурация Vite

```bash
# Проверьте, что plugin включен для production
grep -A 3 "modernImagesPlugin" vite.config.ts
# ✅ Должно быть: enabled: mode === 'production'
```

### 3. Vercel конфигурация

```bash
# Проверьте buildCommand
grep "buildCommand" vercel.json
# ✅ Должно быть: "buildCommand": "npm run build"
```

### 4. Изображения существуют

```bash
# Проверьте наличие исходных изображений
ls -lh src/assets/projects/*.jpg | wc -l
ls -lh public/lovable-uploads/*.png | wc -l
# ✅ Должны быть файлы
```

## 🚀 Процесс деплоя

### Вариант 1: Автоматическая генерация на Vercel (рекомендуется)

```bash
# Просто push - Vercel сделает всё сам
git add .
git commit -m "Ready for deployment"
git push
```

**Что произойдёт:**
1. Vercel запустит `npm install` → установит Sharp
2. Vercel запустит `npm run build` → Production mode
3. Vite plugin автоматически сгенерирует WebP/AVIF
4. Deployment завершится с оптимизированными изображениями

### Вариант 2: Pre-генерация локально (для responsive)

```bash
# 1. Сгенерируйте responsive версии
node scripts/generate-responsive-images.js

# 2. Проверьте результат
ls -lh src/assets/projects/*-{400,800,1200,1600}w.webp | head -5

# 3. Закоммитьте
git add src/assets/
git commit -m "Add responsive images"
git push
```

## 📊 Проверка после деплоя

### 1. Логи Vercel

✅ **В логах деплоя должно быть:**
```
🚀 Starting Modern Image Format Generation
📦 Found XX images to process
✅ Processed: XX images
💾 Average savings: XX%
```

### 2. Chrome DevTools

```bash
# 1. Откройте ваш сайт
# 2. DevTools (F12) → Network → Img
# 3. Перезагрузите страницу (Ctrl+R)
# ✅ Должны загружаться .webp или .avif файлы
```

### 3. Lighthouse

```bash
# Chrome DevTools → Lighthouse → Generate report
# ✅ Performance Score: 90+
# ✅ LCP: < 2.5s
# ✅ Properly sized images: PASSED
```

## ⚠️ Troubleshooting

### Проблема: Sharp не найден

**Симптомы:** Error: Cannot find module 'sharp'

**Решение:**
```bash
# Sharp должен быть в dependencies, НЕ devDependencies
# Vercel устанавливает только dependencies в production
```

### Проблема: Изображения не генерируются

**Симптомы:** Загружаются .jpg вместо .webp

**Проверьте:**
1. Vite собирается в production mode? (проверьте логи)
2. Plugin включен для production? (проверьте vite.config.ts)
3. Исходные изображения существуют? (проверьте src/assets/)

**Решение:**
```bash
# Попробуйте локально
NODE_ENV=production npm run build
# Должны появиться .webp и .avif файлы в dist/
```

### Проблема: Timeout при сборке

**Симптомы:** Vercel deployment failed - Function invocation timeout

**Причина:** Слишком много изображений для генерации

**Решение:**
```bash
# Генерируйте локально и коммитьте
node scripts/generate-modern-formats.js
node scripts/generate-responsive-images.js
git add src/assets/ public/lovable-uploads/
git commit -m "Add pre-generated images"
git push
```

## 📈 Ожидаемые результаты

После успешного деплоя:

| Показатель | Результат |
|------------|-----------|
| 📦 Размер изображений | -60-85% |
| ⚡ Время загрузки страницы | -40-60% |
| 🎯 Lighthouse Performance | 90-95 |
| 🖼️ LCP | < 2.5s |
| 📱 Мобильный опыт | Значительно улучшен |

## 🎯 Готово к деплою?

Пройдитесь по чеклисту:

- [ ] Sharp в dependencies
- [ ] modernImagesPlugin включен для production
- [ ] vercel.json настроен
- [ ] Исходные изображения на месте
- [ ] .vercelignore настроен
- [ ] (Опционально) Responsive версии сгенерированы

**Всё готово?** → `git push` 🚀

---

💡 **Подсказка:** Первый деплой может занять 3-5 минут из-за генерации изображений. Последующие будут быстрее, если изображения не менялись.
